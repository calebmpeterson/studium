import { css } from "@emotion/react";

export const eventLabelCss = css`
  position: absolute;
  display: flex;
  align-items: baseline;
  gap: 5px;
  white-space: nowrap;
  padding: 2px 5px;
  text-shadow: 0 0 5px var(--bg);
`;
